## Packages
framer-motion | Page transitions, staggering animations, and loading states
lucide-react | Icons for UI

## Notes
- The entire application uses a forced dark theme as requested (configured in index.css).
- Polling is implemented for video generation status.
- Standard REST conventions with @tanstack/react-query based on the shared api routes manifest.
